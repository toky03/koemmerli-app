import { SortArticlePipe } from './sort-article.pipe';

describe('SortArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new SortArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
