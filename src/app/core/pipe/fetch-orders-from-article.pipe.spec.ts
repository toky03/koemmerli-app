import { FetchOrdersFromArticlePipe } from './fetch-orders-from-article.pipe';

describe('FetchOrdersFromArticlePipe', () => {
  it('create an instance', () => {
    const pipe = new FetchOrdersFromArticlePipe();
    expect(pipe).toBeTruthy();
  });
});
